# Cassia AutoUI Agent 依赖
# 安装: pip install -r requirements.txt
# 安装后还需执行: playwright install chromium

playwright>=1.40
openai>=1.0
pyte>=0.8
prompt_toolkit>=3.0
rich>=13.0
textual>=0.80
emoji>=2.0

# 测试框架 (pytest-playwright 回归测试)
pytest>=8.0
pytest-playwright>=0.7
pytest-html>=4.0
pytest-rerunfailures>=13.0
pytest-timeout>=2.2
